

//Pasar por defecto la funcion con prevent default y dejar el parametro para los casos de quere un custom handle
export default function CreateButton(){
    
    return(
    
    <button  className="rounded bg-green-600">Crear</button>)
}